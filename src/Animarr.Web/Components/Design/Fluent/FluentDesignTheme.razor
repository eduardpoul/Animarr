@*
    Stub — Fluent's component drove CSS variable selection. We lock dark mode
    via tokens.css + fluent-overrides.css so this is a no-op now.
*@
@namespace Microsoft.FluentUI.AspNetCore.Components

@code {
    [Parameter] public DesignThemeModes Mode { get; set; } = DesignThemeModes.Dark;
    [Parameter] public OfficeColor? OfficeColor { get; set; }
    [Parameter] public string? StorageName { get; set; }
    [Parameter] public EventCallback<DesignThemeModes> ModeChanged { get; set; }
}
