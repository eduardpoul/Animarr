@*
    Stub — Fluent's provider rendered service-shown dialogs at the page root.
    With our shims, dialogs are rendered inline so this provider is a no-op.
    Kept so the existing <FluentDialogProvider /> at the app root still resolves.
*@
@namespace Microsoft.FluentUI.AspNetCore.Components
