@*
    WASM root component. Mounts Animarr.UI's Routes (which in turn renders
    the migrated pages via Blazor's standard Router). All page content lives
    in the RCL so MAUI's BlazorWebView can host the same component.
*@
<Animarr.UI.Components.Routes />
